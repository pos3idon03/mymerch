version: '3.8'
services:
  mongo:
    image: mongo:6
    restart: always
    container_name: b2b-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  app:
    build: .
    container_name: b2b-app
    ports:
      - "5000:5000"
    environment:
      - MONGODB_URI=mongodb+srv://mymerch:161019901804198506032022ArteAnas!@mymerch.lrrohu2.mongodb.net/?retryWrites=true&w=majority&appName=mymerch
      - JWT_SECRET=your-secret-key
      - NODE_ENV=production
    depends_on:
      - mongo
    volumes:
      - ./uploads:/app/uploads

volumes:
  mongo-data: 